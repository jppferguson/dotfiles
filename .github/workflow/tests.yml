name: Dotfiles Tests
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Clone this repository
        uses: actions/checkout@v3
      - name: Run tests
        run: sh src/script/test
