-- Triggers
--   For triggers with no home
-----------------------------------------------
local triggers = {}

-- Add triggers
-----------------------------------------------
triggers.triggers = {}
triggers.triggers["Window Hints"] = hs.hints.windowHints

----------------------------------------------------------------------------
return triggers
